/**
 *
 * Uncomment the lines below to use Prisma with PostgreSQL
 * Rename this file to schema.prisma
 * Run `yarn prisma:generate` to generate the Prisma client
 * Run `yarn prisma:migrate` to migrate your database
 * Run `yarn prisma:studio` to open the Prisma Studio
 *
**/

// generator client {
//   provider = "prisma-client-js"
// }
//
// datasource db {
//   provider  = "postgresql"
//   url       = env("POSTGRES_PRISMA_URL") // uses connection pooling
//   directUrl = env("POSTGRES_URL_NON_POOLING") // uses a direct connection
// }
//
// model User {
//   id        Int      @id @default(autoincrement())
//   name      String
//   email     String   @unique
//   image     String
//   createdAt DateTime @default(now())
// }
